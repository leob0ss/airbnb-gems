# =============================================================================
# Airbnb Gems — Environment Variables
# =============================================================================
# Copy to .env for local dev:  cp .env.example .env
#
# On Vercel: Project Settings → Environment Variables → add each below.
# =============================================================================

# REQUIRED — hosted MySQL (Railway, TiDB Cloud, PlanetScale, Aiven, etc.)
DATABASE_URL=mysql://user:password@host:3306/airbnb_gems

# OPTIONAL — local Express dev server port (default 3000)
PORT=3000

# OPTIONAL — Google Maps (desktop map view). Get key at console.cloud.google.com
VITE_GOOGLE_MAPS_API_KEY=

# OPTIONAL — email notifications for survey / contact / filter requests (Resend)
NOTIFY_EMAIL=you@example.com
RESEND_API_KEY=
RESEND_FROM=Airbnb Gems <notifications@yourdomain.com>

# Local MySQL via docker-compose (alternative to brew install mysql):
# DATABASE_URL=mysql://root:airbnb_gems@127.0.0.1:3306/airbnb_gems

# =============================================================================
# Deploy checklist (Vercel)
# =============================================================================
# 1. Push repo to GitHub
# 2. Import project in Vercel (framework: Other, build: pnpm build:client)
# 3. Set DATABASE_URL + optional vars above
# 4. After first deploy, seed production DB:
#      pnpm db:migrate
#      pnpm db:import -- --file data/listings-export.csv
#    (run locally with DATABASE_URL pointing at production)
